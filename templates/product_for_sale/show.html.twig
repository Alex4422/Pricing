{% extends 'base.html.twig' %}

{% block title %}ProductForSale{% endblock %}

{% block body %}
    <h1>ProductForSale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_for_sale.id }}</td>
            </tr>
            <tr>
                <th>PrixPlancher</th>
                <td>{{ product_for_sale.prixPlancher }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_for_sale_index') }}">back to list</a>

    <a href="{{ path('product_for_sale_edit', {'id': product_for_sale.id}) }}">edit</a>

    {{ include('product_for_sale/_delete_form.html.twig') }}
{% endblock %}
